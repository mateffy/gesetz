{
  "name": "@gesetz/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "gesetz": "./dist/main.js"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "bun build src/main.ts --outdir dist --target bun --format esm --sourcemap --external dependency-cruiser --external eslint --external ts-morph --external tree-sitter --external tree-sitter-php --external @gesetz/core --external @gesetz/typescript --external @gesetz/php --external @gesetz/oxlint --external @gesetz/oxfmt --external @gesetz/vitest --external @gesetz/bun-test --external @gesetz/pest --external @gesetz/prettier --external @gesetz/junit --external @gesetz/effect-ts --external @gesetz/eslint --external @gesetz/phpstan --external @gesetz/storybook --external @gesetz/phpunit --external effect --external @effect/cli --external @effect/platform --external @effect/platform-node --external @effect/platform-node-shared --external @effect/printer --external @effect/printer-ansi",
    "dev": "bun run src/main.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@effect/cli": "^0.55.0",
    "@effect/platform": "^0.80.0",
    "@effect/platform-node": "^0.73.0",
    "@gesetz/core": "workspace:*",
    "@gesetz/typescript": "workspace:*",
    "@gesetz/php": "workspace:*",
    "effect": "^3.15.0"
  },
  "devDependencies": {
    "@types/node": "22.0.0",
    "typescript": "^5.8.0",
    "vitest": "^4.0.0"
  }
}
